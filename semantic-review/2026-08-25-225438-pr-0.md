# Fail-closed Task 2 provenance and readiness contracts

This follow-up verifies the five requested fixes in the Task 2 artifact/readiness change. The prior blockers around canonical gates, claim-to-provenance binding, Git-backed clean-source identity, and timestamp semantics are fixed. E20 remains crossing-only and `passed` now equals all checks the verifier requires, but the archive can redefine that required set and silently remove architecture or confidence-bound coverage (**Medium, confirmed**). E19/E20’s structured unreconstructible-source flags are fail-closed, while the human-facing regeneration interpretation still accepts a negated statement (**Low, confirmed**). Targeted validation passed 13/13; the full-suite result supplied for this pass is 232 passed with 4 warnings.

Watch for: **Medium, confirmed** — E20 retains `passed: true` after coherently deleting all GRU checks or both bootstrap confidence-bound checks; **Low, confirmed** — a sentence saying regeneration is “not a new study” passes the historical-regeneration interpretation guard.

**Verdict**: NEEDS_CHANGES

## High-level view

The readiness schema now owns the complete ordered gate set and each gate’s mandatory/optional classification. Deleting, adding, reordering, or downgrading a gate is rejected before readiness is computed, and new-outcome blockers are derived from fixed gate identifiers.

The claim validator now binds E19/E20 archive roots, evidence digests, archive-manifest digests, frozen-file counts, source-provenance flags, accepted outputs, and pass/fail state to central provenance. The structured fields preserve unavailable, unreconstructible dirty source, but the free-text interpretation guard can be defeated by negation (**Low, confirmed**).

E20’s accepted outputs are fixed to `drift_crossing` and `response_crossing`, and its pass bit is the conjunction of the required checks. Model families and bootstrap requirements are nevertheless read from mutable archive content, allowing the positive result to survive a reduced test surface (**Medium, confirmed**).

A `clean_git_source` declaration now resolves through Git and recomputes the executable-source digest and file count from committed `run_experiment.py` and `src/**/*.py` blobs. The archived authoritative fingerprint independently matches the 17 files at commit/tag `2d0ee83`; E19/E20 remain explicitly historical and unreconstructible.

The 2026-08-24 timestamp is now named `initial_schema_v1_generated_utc`; schema v2 carries a null update timestamp and an explicit note that no replacement time is asserted. It no longer claims to date the later E19/E20 index.

<details>
<summary>Issues (2)</summary>

1. **Medium / confirmed — Archive-defined E20 acceptance surface** — Mutable preflight model kinds and an optional threshold determine which checks are mandatory, so coherently removing GRU or bootstrap checks still validates. Freeze E20’s tanh/GRU design, record count, threshold keys, and exact check names outside the archive, then test fully rehashed deletions.
2. **Low / confirmed — Negatable replay interpretation** — `required_interpretation` only has to contain `new study`, so text claiming regeneration is “not a new study” validates. Replace this prose test with an exact enum or canonical value and add a negation regression.

</details>

<details><summary>Details</summary>

## E20 acceptance remains partly self-defining — Medium (**confirmed**)

`verify_archive_manifest` builds per-model requirements from `model_kind` values in the preflight records and adds bootstrap confidence-bound checks only when `thresholds.minimum_two_way_bootstrap_ci95_low` is non-null (`verify_release_artifacts.py:577-600`). Neither the claim schema nor central provenance independently freezes the expected model families or threshold/check contract.

A coherent mutation changed every preflight record’s `model_kind` to `tanh`, removed the `gru_*` checks, updated the manifest sidecar and evaluation identities, and rehashed the archive; verification still returned `passed: true`. A second coherent mutation removed `minimum_two_way_bootstrap_ci95_low` and both `overall_*_bootstrap_ci95_low` checks, updated `metrics.json` and the archive manifest, and also passed. These mutations retain the two crossing output names while weakening the acceptance surface.

Freeze E20’s expected model families and complete check/threshold schema independently of the archive under verification. Validate the claim’s 64-record tanh/GRU design and bootstrap criterion against the preflight and acceptance seals, with coherent-rehash regression cases.

## Historical replay prose can contradict its structured flags — Low (**confirmed**)

The structured historical-source booleans are pinned false where required, but `required_interpretation` only needs to contain the lowercase substring `new study` (`scripts/check_submission_readiness.py:302-306`). “Historical regeneration is not a new study and may be represented as a replay” therefore validates.

Represent the interpretation with an exact enum such as `regeneration_class: new_study`, or pin canonical wording if the prose is part of the reviewed contract.

</details>

<details>
<summary>File map</summary>

- `scripts/check_submission_readiness.py` — canonical gate schema and claim-to-provenance bindings.
- `research_scope/submission_claim_set.yaml` — frozen empirical scope and historical-source disclosure.
- `research_scope/submission_readiness.yaml` — current fail-closed submission and new-outcome decisions.
- `verify_release_artifacts.py` — archive semantics and Git-backed source reconstruction.
- `paper/artifacts/provenance_manifest.json` — E19/E20 evidence index and corrected timestamp semantics.
- `paper/artifacts/enl_ntk_pilot-20260825/` — failed broad-pilot compact archive.
- `paper/artifacts/enl_ntk_crossing_factorial-20260825/` — restricted crossing-event compact archive.
- `tests/test_submission_readiness.py` — gate and cross-document mutation coverage.
- `tests/test_release_artifacts.py` — acceptance and source-provenance mutation coverage.
- `.github/workflows/tests.yml` — pre-install manifest/artifact checks and clean-tree enforcement.

Full review input: `git diff main` for tracked changes plus the untracked Task 2 files reported by `git ls-files --others --exclude-standard`.

</details>
