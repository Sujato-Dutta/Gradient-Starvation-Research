# New parameter-prediction proof: Phase-1 scope audit

Source: user-supplied `parameter_prediction_proofs.pdf` (58 pages, dated 12 September 2026). Status: **statement/assumption review only, not an independent verification of all proofs or computer-assisted certificates**.

| Item | What the PDF states | Submission handling |
|---|---|---|
| Prospective parameter-ball/warm-up certificates | Conditional inequalities for full-batch logistic gradient flow and explicit full-batch GD in stated finite-dimensional parameter charts | Keep hypotheses, certified horizon and strict margin beside each conclusion; do not describe as a generic predictor for ordinary CNN optimization. |
| Constructed RNN/tanh-CNN examples | Exact rational/interval-assisted existence certificates; a very small `~1e-7` response deficit in the shipped examples | The five-parameter tanh-CNN example is now included as a proof-package mathematical certificate. The manuscript reproduces its exact logits, assumptions and rational certificate arithmetic, and labels it as constructed existence with a tiny margin and short horizon. It is not presented as an independently rerun computer-assisted artifact. |
| CIFAR/ordinary training | No universal conclusion for minibatch, momentum/AdamW, activation switches, or standard CIFAR models | Treat Phase-2 CIFAR as empirical validation of the diagnostic protocol, not as a theorem test whose success is guaranteed. |
| Nonvacuity | PDF pp. 56–58 reports margins about `1e-4` of contemporaneous W levels, both models classifying the noiseless probes correctly, and 0/30 plus 0/25 sampled parameter points certified | Report these limits prominently; do not imply practical behavioral harm or typical-initialization coverage from the constructed examples. |
| Hitting-time stability | The current `research_scope/e2_theorem.md` Corollary D already includes absolute continuity, an almost-everywhere lower derivative bound, prehistory separation and grid error | Check that the final manuscript theorem/appendix retains all these assumptions. The proof PDF and paper may have different numbering. |
| Causal endpoint | PDF p. 6 Eq. E10 calls a W gate plus **any negative gap at some positive time** a certificate | **Resolved:** corrected `paper/main.tex` uses this broad endpoint as Definition 1 and names negativity at W's first hit as the stricter **at-hit** subtype. The prospective theorem proves the broad endpoint and states an extra timing condition sufficient for at-hit. |
| Computer-assisted trust boundary | PDF p. 46 describes `code/reproduce.py`, `general_prediction.py`, `nonlinear_models.py`, exact intervals and SMT checks, but states no end-to-end formal proof | The companion source/config/certificate package is not in the repository or among the attached files. The manuscript can reproduce and expose the stated exact arithmetic, but the interval-generation reports have not been independently rerun. Obtain that package and an independent mathematical review before describing the certificate as independently reproduced. |

The manuscript now states the tanh-CNN construction with a common B/W probe, shared initialization, all five trainable coordinates, uniform parameter-ball bounds, exact rational margins, and a matched no-suppression control. It also states the crucial limitation: this is the proof package's constructed broad certificate, not the stricter at-hit subtype, a typical-initialization result, or evidence of practical-scale harm. The companion certificate-generating source and solver-version record remain unavailable, so independent rerunning and mathematical review are still required. `research_scope/parameter_prediction_theorem.md` is an older local note, not a substitute for that package.
